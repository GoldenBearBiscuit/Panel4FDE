# ── Java / Maven ──
target/
*.class
*.jar
*.war
!.mvn/wrapper/maven-wrapper.jar

# ── Node ──
node_modules/
dist/
.vite/
*.tsbuildinfo

# ── 日志与临时文件 ──
*.log
logs/
*.tmp
*.swp

# ── IDE / 系统 ──
.idea/
.vscode/
*.iml
*.ipr
*.iws
.DS_Store
Thumbs.db

# ── 容器 ──
# 数据卷由 docker 管理，不在仓库里；mysql 数据目录若被挂出则忽略
docker/mysql-data/
docker/redis-data/

# ── 注意 ──
# tools/e2e/shots/ 的验收截图【故意保留在版本库里】：
# 它们是「阶段一已验收」的证据，且体积很小（约 200KB）。
# 需要重新生成时跑 cd tools/e2e && node accept.mjs
